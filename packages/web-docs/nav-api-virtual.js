// Fallback for `@opentf/web-docs/nav-api`.
//
// When an `api` section is configured, a second `docsNavPlugin` instance intercepts
// this specifier and replaces it with the generated API navigation tree. This file is
// loaded only when that plugin is NOT active, in which case the nav is empty.

export default [];
