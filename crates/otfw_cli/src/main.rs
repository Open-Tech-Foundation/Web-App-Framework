//! OpenTF Web toolchain CLI (`otfw`).
//!
//! Status: foundation placeholder. The orchestrator (dev server, build, HMR,
//! incremental cache) described in `ARCHITECTURE.md` §8 is implemented here.

fn main() {
    println!("otfw: OpenTF Web toolchain (foundation). See ARCHITECTURE.md.");
}
