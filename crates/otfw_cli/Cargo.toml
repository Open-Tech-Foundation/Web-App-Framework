[package]
name = "otfw_cli"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "OTF Web — the IR compiler binary (otfwc), invoked by the toolchain. See ARCHITECTURE.md §8."

[[bin]]
name = "otfwc"
path = "src/main.rs"

[dependencies]
otfw_compiler = { workspace = true }
