[package]
name = "otfw_cli"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "OpenTF Web — toolchain CLI entry point. See ARCHITECTURE.md §8."

[[bin]]
name = "otfw"
path = "src/main.rs"

[dependencies]
otfw_compiler = { workspace = true }
