[package]
name = "otfw_ir"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "OpenTF Web — intermediate representations (the compiler backbone). See ARCHITECTURE.md §4."

[dependencies]
# No external deps yet. Serialization (serde) is added when artifact format is settled (ARCHITECTURE.md §5.6).
