---
title: Installation
description: Scaffold a new OTF Web project and start the dev server.
---

import { Callout, Tabs, Steps } from "@opentf/web-docs";

export const scaffoldTabs = [
  { label: "bun", content: "bun create @opentf/web my-app" },
  { label: "npm", content: "npm create @opentf/web@latest my-app" },
  { label: "pnpm", content: "pnpm create @opentf/web my-app" },
  { label: "yarn", content: "yarn create @opentf/web my-app" },
];

# Installation

<Steps>

### Scaffold a project

Create a new project with the initializer. It's interactive — it sets up the project
and lets you pick a styling solution and optional modules. Use whichever package
manager you prefer:

<Tabs tabs={scaffoldTabs} />

### Install dependencies

```bash
cd my-app
bun install
```

### Start the dev server

```bash
bun run dev
```

The dev server compiles on demand and hot-reloads on save.

</Steps>

<Callout type="tip" title="Requirements">
  OTF Web targets modern browsers and runs its toolchain on Bun or Node 18+.
</Callout>

## Production build

Build a static bundle you can host anywhere:

```bash
bun run build
```

For a fully pre-rendered site, see [Static Generation](/docs/guides/static-generation).

## Editor setup

The compiler uses a custom JSX pipeline. Point your `jsconfig.json` (or
`tsconfig.json`) at it so the language server understands your components:

```json jsconfig.json
{
  "compilerOptions": {
    "jsx": "preserve",
    "jsxImportSource": "@opentf/web"
  }
}
```

<Callout type="info" title="Why jsxImportSource">
  It tells your editor where JSX types come from, so autocomplete and type-checking
  match what the compiler actually emits — without affecting the build.
</Callout>
