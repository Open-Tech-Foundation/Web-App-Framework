{
  "name": "website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "EXCLUDE_ROUTES=forms-demo,web-form otfw dev",
    "build": "EXCLUDE_ROUTES=forms-demo,web-form otfw build",
    "build:ssg": "EXCLUDE_ROUTES=forms-demo,web-form otfw build --ssg"
  },
  "dependencies": {
    "@opentf/web": "^0.5.0"
  },
  "devDependencies": {
    "@opentf/web-cli": "^1.0.0"
  }
}
