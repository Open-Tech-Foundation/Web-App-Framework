{
  "name": "web-app-framework",
  "repository": {
    "type": "git",
    "url": "https://github.com/Open-Tech-Foundation/Web-App-Framework.git"
  },
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground",
    "website",
    "benchmarks/*"
  ],
  "scripts": {
    "dev": "bun run --cwd playground dev",
    "bench": "bun benchmarks/run.mjs",
    "playground:build": "bun run --cwd playground build",
    "preview": "bun run website:build && wrangler dev",
    "cf:build": "bun scripts/build-website-standalone.mjs",
    "website:dev": "bun run --cwd website dev",
    "website:build": "bun run build:web && bun run --cwd website build",
    "test": "cargo build && cargo test && bun test packages/web/ packages/web-cli/tests/ packages/create-web/tests/",
    "deploy": "bun run website:build && wrangler deploy",
    "build:web": "bun run --cwd packages/web build"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.33.1",
    "@opentf/std": "^0.13.0",
    "@rollup/plugin-babel": "^7.0.0",
    "@tailwindcss/vite": "^4.2.2",
    "@types/bun": "latest",
    "jiti": "^2.6.1",
    "linkedom": "^0.18.12",
    "rolldown": "1.0.0-rc.17",
    "tailwindcss": "^4.2.2",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "wrangler": "^4.84.1",
    "zod": "^4.3.6"
  }
}
